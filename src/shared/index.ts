import styled from 'styled-components'

const Title = styled.h2`
  font-size: 30px;
`

const SectionWrapper = styled.div`
  width: 100%;
  position: relative;
`

const TextBlock = styled.p`
  margin-bottom: 30px;
`

export {SectionWrapper, Title, TextBlock}
